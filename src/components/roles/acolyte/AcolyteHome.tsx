import ScreenContainer from '../../ScreenContainer';
import { ScreenBackgroundImgSrc } from '../../../constants/image-sources';

const AcolyteHome = () => {
  return (
    <ScreenContainer backgroundImgSrc={ScreenBackgroundImgSrc.ACOLYTE_HOME}>
      {/* TODO: Insert screen content */}
    </ScreenContainer>
  );
};

export default AcolyteHome;

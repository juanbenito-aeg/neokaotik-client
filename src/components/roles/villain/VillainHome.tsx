import ScreenContainer from '../../ScreenContainer';
import { ScreenBackgroundImgSrc } from '../../../constants/image-sources';

const VillainHome = () => {
  return (
    <ScreenContainer backgroundImgSrc={ScreenBackgroundImgSrc.VILLAIN_HOME}>
      {/* TODO: Insert screen content */}
    </ScreenContainer>
  );
};

export default VillainHome;

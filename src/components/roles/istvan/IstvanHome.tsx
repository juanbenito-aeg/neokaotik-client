import ScreenContainer from '../../ScreenContainer';
import { ScreenBackgroundImgSrc } from '../../../constants';

const IstvanHome = () => {
  return (
    <ScreenContainer backgroundImgSrc={ScreenBackgroundImgSrc.ISTVAN_HOME}>
      {/* TODO: Insert screen content */}
    </ScreenContainer>
  );
};

export default IstvanHome;
